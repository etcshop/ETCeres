{% extends getPartial('page-design') %}
{% block PartialHead %}
    <title>{% if category.details[0].metaTitle %}{{ category.details[0].metaTitle }}{% else %}{{ category.details[0].name }}{% endif %} | {{ config("Ceres.header.company_name") }}</title>
{% endblock %}
{% block PageBody %}
    {% set shopBuilderTemplate = shop_builder_category_template(category.id) %}
    {% if shopBuilderTemplate | trim is not empty %}
        <div class="container-max shopbuilder_template">
            <div class="row">
                <div class="col">
                    {{ shopBuilderTemplate | raw }}
                </div>
            </div>
        </div>
    {% else %}
      <div class="category_template"></div>
        <div>{{ category.id }}</div>
        <div>{{ lang }}</div>
        <div>{{ webstoreConfig.webstoreId }}</div>


          <div>{{ category | raw }}</div>
          <div>{{ category | json_encode }}</div>
        {% include category_template( category.id, lang, webstoreConfig.webstoreId ) ignore missing %}
    {% endif %}

{% endblock %}
